# next-auth@5 (beta) declares an OPTIONAL peer on @simplewebauthn/browser@^9
# (its experimental built-in WebAuthn provider). This project uses its own
# WebAuthn implementation (@simplewebauthn/server v11) instead, so the newer
# browser package is intentional. Allow the install to proceed.
legacy-peer-deps=true
